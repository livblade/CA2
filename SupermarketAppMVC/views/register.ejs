<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <title>Register - Ethan's Grains</title>
</head>
<body>
  <!-- Navigation bar with Ethan's Grains branding -->
  <nav class="navbar navbar-expand-sm navbar-custom">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <span class="brand-icon">🏪</span> Ethan's Grains
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/register">Register</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <!-- Dark mode toggle button -->
            <button id="theme-toggle" class="theme-toggle nav-link">🌙 Dark Mode</button>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <!-- Registration form card -->
        <div class="form-container">
          <h2 class="text-center mb-4" style="color: var(--primary-blue); font-weight: 700;">Create Account</h2>
          
          <!-- Display error messages if validation fails -->
          <% if (messages && messages.length > 0) { %>
            <% messages.forEach(function(message) { %>
              <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= message %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
              </div>
            <% }); %>
          <% } %>

          <!-- Registration form - submits to /register POST endpoint -->
          <form action="/register" method="POST">
            <!-- Username field -->
            <div class="mb-3">
              <label for="username" class="form-label filter-label">Username</label>
              <input type="text" id="username" name="username" class="form-control" required 
                     value="<%= (formData && formData.username) ? formData.username : '' %>">
            </div>

            <!-- Email field -->
            <div class="mb-3">
              <label for="email" class="form-label filter-label">Email</label>
              <input type="email" id="email" name="email" class="form-control" required 
                     value="<%= (formData && formData.email) ? formData.email : '' %>">
            </div>

            <!-- Password field - minimum 6 characters required by validation -->
            <div class="mb-3">
              <label for="password" class="form-label filter-label">Password</label>
              <input type="password" id="password" name="password" class="form-control" required>
              <small class="text-muted">Minimum 6 characters</small>
            </div>

            <!-- Address field -->
            <div class="mb-3">
              <label for="address" class="form-label filter-label">Address</label>
              <input type="text" id="address" name="address" class="form-control" required 
                     value="<%= (formData && formData.address) ? formData.address : '' %>">
            </div>

            <!-- Contact number field -->
            <div class="mb-3">
              <label for="contact" class="form-label filter-label">Contact Number</label>
              <input type="text" id="contact" name="contact" class="form-control" required 
                     value="<%= (formData && formData.contact) ? formData.contact : '' %>">
            </div>

            <!-- Role selection - REMOVED admin option, all signups are users -->
            <input type="hidden" name="role" value="user">

            <!-- Submit button -->
            <button type="submit" class="btn btn-success w-100">Register</button>
          </form>
          
          <!-- Link to login page for existing users -->
          <p class="text-center mt-3 login-footer-text">
            Already have an account? <a href="/login" class="login-link">Login here</a>
          </p>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/theme.js"></script>
</body>
</html>
