<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <title>Edit User - Ethan's Grains</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-custom">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <span class="brand-icon">🏪</span> Ethan's Grains
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/inventory">Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/users">Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
          <li class="nav-item">
            <button id="theme-toggle" class="theme-toggle nav-link">Dark Mode</button>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="page-header">
      <h2>Edit User</h2>
    </div>

    <% if (userData) { %>
      <form action="/users/update/<%= userData.id %>" method="POST" class="form-container">
        
        <div class="mb-3">
          <label for="username" class="filter-label">Username</label>
          <input type="text" class="form-control" id="username" name="username" 
                 value="<%= userData.username %>" required>
        </div>

        <div class="mb-3">
          <label for="email" class="filter-label">Email</label>
          <input type="email" class="form-control" id="email" name="email" 
                 value="<%= userData.email %>" required>
        </div>

        <div class="mb-3">
          <label for="password" class="filter-label">New Password (leave blank to keep current)</label>
          <input type="password" class="form-control" id="password" name="password">
          <small class="text-muted">Only fill if you want to change the password</small>
        </div>

        <div class="mb-3">
          <label for="address" class="filter-label">Address</label>
          <input type="text" class="form-control" id="address" name="address" 
                 value="<%= userData.address || '' %>">
        </div>

        <div class="mb-3">
          <label for="contact" class="filter-label">Contact Number</label>
          <input type="text" class="form-control" id="contact" name="contact" 
                 value="<%= userData.contact || '' %>">
        </div>

        <div class="mb-4">
          <label for="role" class="filter-label">Role</label>
          <select class="form-select" id="role" name="role" required>
            <option value="user" <%= userData.role === 'user' ? 'selected' : '' %>>User</option>
            <option value="admin" <%= userData.role === 'admin' ? 'selected' : '' %>>Admin</option>
          </select>
        </div>

        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-success">💾 Update User</button>
          <a href="/users" class="btn btn-outline-primary">Cancel</a>
        </div>
      </form>
    <% } else { %>
      <div class="empty-state">
        <div class="empty-state-icon">❌</div>
        <h3>User not found</h3>
        <p>The requested user could not be found.</p>
        <a href="/users" class="btn btn-primary">Back to Users</a>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/theme.js"></script>
</body>
</html>
